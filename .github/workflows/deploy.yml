name: Deploy PHP Site to IONOS

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v3

      - name: Vérifier si REMOTE_HOST est défini
        run: |
          if [ -z "${{ secrets.REMOTE_HOST }}" ]; then
            echo "🚨 ERREUR: REMOTE_HOST est vide ou non défini dans GitHub Secrets"
            exit 1
          else
            echo "✅ REMOTE_HOST = '${{ secrets.REMOTE_HOST }}'"
          fi

      - name: Configurer SSH
        run: |
          mkdir -p ~/.ssh
          printf "%s\n" "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -4 -H "${{ secrets.REMOTE_HOST }}" >> ~/.ssh/known_hosts
          ls -lah ~/.ssh && cat ~/.ssh/id_rsa || echo "🚨 Clé SSH non trouvée !"

      - name: Tester la connexion SSH en mode debug
        run: |
          ssh -vvv -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no u71988742@"${{ secrets.REMOTE_HOST }}" "echo 'Connexion réussie !'"
